/// <reference types="vite/client" />

// @fontsource-variable/inter ships CSS only (no type declarations); declare the
// side-effect import so the renderer typecheck passes.
declare module '@fontsource-variable/inter'
