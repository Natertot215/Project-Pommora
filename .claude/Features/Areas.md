### Areas

**Areas are tier-1 Contexts** — free-standing folders at `.nexus/areas/<Title>/` with an `_area.json` sidecar, alongside Topics and Projects. This doc is a pointer; the full spec lives in [[Contexts]].

→ [[Contexts]] — full spec for Areas, Topics, Projects
→ [[Homepage]] — singleton dashboard entity (distinct from an Area)
