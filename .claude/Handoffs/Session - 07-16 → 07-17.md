**Session ID:** 1968ae09-ee23-4a88-9c0d-3a665384fd8e
**Dates:** 07-16-2026 → 07-17-2026

> ⚡ **Cornerstone — carry into every handoff, unchanged (Nathan's voice).**
> *"You do NOT guess — you LOOK, and you ASK. Open the file and read the code before you assert anything; ask me when you're unsure. A plan built on an unverified claim is a liability, not progress — treat every doc, every `file:line`, every 'it works like X' as a hypothesis until you've read the code that proves it. Honesty over confidence; confidence is earned through evidence."*

**Date:** 07-16-2026 → 07-17-2026
**Model:** Fable 5
**Compactions:** 2
**Connectors:** none
**Commands:** /code-simplification, /code-review, /compact, /handoff
**Agents:** build-breaking-agent (7x - spec+plan attacks), Explore (2x - reuse maps), general-purpose (1x - transcript miner)
**Skills:** studio-brainstorm, superpowers:writing-plans, handoff

The session that took Page Previews from a parked config value to a certified, execution-ready feature cycle — with the Multi-Tab Nexus merge and a live-driven interactive shell along the way.

**Multi-Tab Nexus shipped:** merged to main (70ce6045) after simplifier + breaker passes; the bidirectional-glyph CM6 bug turned out to be an inline-block mark inheriting the list line's hanging `text-indent` — fixed with `text-indent: 0` on `.md-sym-bidir`. Biome was settled repo-wide (the hook's load-bearing fix is `cd`-ing into the config dir) and ClaudeMD leaned to one Important Information line.

**The Page Previews brainstorm ran the full studio-brainstorm loop:** grounding → interrogation → Nathan's tab-model pivot (previews became a semi-multi-tabbed mini-app; chevrons died; NavWindow became a perma-pinned map tab in the same chrome) → the meticulous interaction matrix. Everything lives in [[Page Previews — Decision Log]] — it is the single spec of record; its Shipped Shell section separates built from to-build.

**The shell was built mid-brainstorm at Nathan's explicit gate-waiver** so he could live-drive UIX: `PagePreview/PreviewWindow.tsx` on the extracted `useFloatingWindow` chrome, B-1 routing at TableView's title click, native right-click "Open in Preview" (wikilinks via the new `conn-menu` IPC, sidebar via the existing native menu, nav rows in-renderer), edge-to-edge tinted frost, floating toolbar with the `--io` icon swallow, the rail-material resizable inspector scaffold. Nathan tuned knobs live; every interjection was folded and committed (~a dozen fix commits, `532b84d7..f327ba4f`).

**The overnight cycle (Nathan's directive, executed while he slept):** spec attack (4 lenses + a transcript miner, ~40 findings, every one self-verified at its citation) → fold to V2 → round 2 found a real HIGH (the adopt guard trusted an unmount flush that `useExitPresence` defers past the root flip) → the awaitable `registerPreviewFlush` fix shipped green → round 3 certified (edc412b1). Three data-safety guards shipped along the way: applyTree reconciles `previewTarget` (D-6), nexus adopt closes + flushes the preview (D-9), and the flush-ordering fix. Then the 9-phase implementation plan was written and certified through 2 attack rounds (ef22b270) — round 1's headline catch was a green-but-dead hazard (openPreviewTab store-tested but never rewired into PreviewWindow's connections handler).

**Lessons Learned**
- Back-propagate spec pivots into summary sections immediately — round 1's dominant finding class was singleton-era law surviving in Frame/Core/Sweep after the tab-model pivot.
- "Clone the pattern" isn't a spec when the shape changes: tabs.json carries no cold-set reconcile because it has no cold sets.
- A non-overflowing scroll container with `overscroll-behavior: none` eats the wheel instead of chaining — the preview scroll-death cause.
- Review agents' "assign behavior to a task whose file list can't deliver it" is a recurring plan-failure class — check file scope against every stated interface.

**Key Files & Insights**
- [[Page Previews — Decision Log]] — certified spec (3 rounds). [[Page Previews — Implementation Plan]] — certified plan (2 rounds, 9 phases). [[Page Previews — Execution Prompt]] — the post-compact resume contract.
- `Detail/pageFlush.ts` now carries both editors' awaitable flushes; PageEmbed's `registerFlush` is the preview's opt-in.
- `design-system/interactions/FloatingWindow.tsx` — the shared window chrome (per-window-id geometry); NavWindow rebases onto it in plan Phase 8.

**Landmines**
- The preview's editor renders the full doc (no CM6 viewport virtualization) — huge-page stance is a plan-stage decision (flagged in the log's E-sweep).
- Nathan's dev session needs a restart to pick up the native-menu main-process code (conn-menu, sidebar item) — everything renderer-side is already live via HMR.

**User Feedback**
- Screenshots reach Nathan's phone only when the agent READS them — never SendUserFile.
- Design interjections are law and fold immediately; state an objection once, then build and show.

**Uncertain**
- The [assumed] set awaiting Nathan's morning pass (all flagged in the log + execution prompt): I-25 nav-row in-renderer carve-out, H-9 banner'd morph variant, H-2 map-tab content model, H-10 no-auto-summon, B-6 SettingsPane row shape, B-7 placeholder dismiss.

---

### Working Notes

- Launch: `env -u ELECTRON_RUN_AS_NODE npm run dev`; CDP screenshots → Read the PNG (phone-visible); zero-mutation drive recipes in project memory.
- Gates: `set -o pipefail; npm run typecheck && npx vitest run && npm run build` — background, read summary lines.

### Next Session

- Execute the certified plan phase-by-phase per [[Page Previews — Execution Prompt]] (Phase Protocol: gates → breaker → simplifier → commit → ping → plan re-read). Start at the first unchecked task (Phase 1: the preview tab model).
- Nathan wakes to post-plan UIX tweaks — fold them as law, batch-commit, keep executing.

### Pending Focuses

- The final report (plan Phase 9.2) — every knob, every design decision, every assumption — delivered at the very END, nowhere else.
- Deferred scoping (pre-existing): v0.5.0 Views Complete brainstorm, NavPane dropdown content, User Sections CRUD, Canvas spec review.

### Fix Log

- (empty — the three preview data-safety fixes shipped and are recorded in History via the plan's Phase 9)

### Handoff Rules

- One block per session, updated in place across compactions; bump the Compactions count.
