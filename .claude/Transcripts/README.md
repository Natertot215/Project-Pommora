### Transcripts

Summarized session transcripts are stored here with naming: `MM-DD (#).md` where `#` is the session number for that date.

Transcripts are created manually via command — do not auto-generate.

#### Sessions
[List of session dates and brief summaries]
