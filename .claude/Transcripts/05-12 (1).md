<!-- session: dc67964c-b1da-4678-a9de-bab96ee6e564 -->

# Session — 05-12 (Spec Pass + Stack Call)

## What we started with

Pommora at the design-system spec phase. Prior commits had landed the Figma build brief at `.claude/ Planning/Figma Prompt.md`, the `UI-UX//` folder with guideline docs, and locked construct decisions (callouts as `:::callout`, blockquotes with left bar, code tokens, foldable headings). Items entity already in the domain model. Stack still undecided — React+Electron and SwiftUI both live. No code yet; `.claude//` contains specs only.

Nathan opened the session with two corrections to the Figma Prompt: add a "shapes" concept for shared border radii in px, and strip all SwiftUI / cross-stack mentions from that one doc while keeping the broader project stack-uncertain.

## What happened along the way

The session ran as roughly thirteen correction passes across the docs, ending in a stack recommendation.

**Figma Prompt updates:** added a Shapes section with pinned px values (`square 0`, `tight 4`, `surface 8`, `pill 9999`), renamed `radius//` → `shape//` throughout, then added a labeling requirement (every primitive/component/variant text-labeled, every variable in a documentation frame). SwiftUI / dual-export language stripped from this doc only. Nathan caught scope drift mid-pass: > "stack is still uncertain, only the prompt treats react as certain, don't adjust anything wlse"

**Tiptap framing corrected.** Earlier docs treated Tiptap as "removed from candidacy" on commercial-trajectory grounds. Nathan: > "Fix TipTap framing for React — its open-source free version does what BlockNote does and possibly better; going with React means both are equally viable." Rewrote across ReactInfo, Resources, History, PRD, Handoff, Pages, Architecture, Domain-Model, Prospects, v0.0, SwiftInfo — BlockNote and Tiptap reframed as co-primary candidates.

**"Lossy" framing flipped** across three docs from a defensive "it could matter but doesn't" to a tight "non-issue, quick fix" disposition.

**Block-style editor UX corrected as WANTED on React** — biggest framing fix of the session. Earlier docs said "drag handles disabled for prose-first feel," which got Notion-style block UI on the cut list. Nathan: > "block-style editing surface on a non-block item like markdown is exactly what i'd want in react." Per-paragraph `+` / drag-handle markers reframed as the affordance Pommora wants on React; SwiftUI path still allowed to forgo them as a native-text-engine constraint. Rewrote across ReactInfo, Pages, Handoff, PRD, Domain-Model, Architecture, Resources.

**Honest read of v1 gaps + stack blockers** delivered. Surfaced ~20 spec gaps; Nathan resolved six directly: backlinks isn't a feature (just links); inspector shows property panel by default with a CLI-frontend AI chat coming later; trash to `<vault>/.trash/`; move-strip triggers a confirmation warning; no vault folder suggestion at first launch; invalid filenames out of scope; Saved pinning out of v1 scope.

**Items have an interface.** Nathan: > "ask me questions before implementing. Items are not non-interface; they have a simple pop-up menue-like window with its title, property and a place for about 150-250 characters for descriptions. They're like when you click on an event in a calendar app — not a full page but they do have an interface." Asked three clarifying questions before implementing. Locked: **Item window** naming, popover anchored to trigger (Calendar-event-detail pattern), title + property inputs + 250-char hard-capped plain-text description. Replaces the prior "Items open in the inspector" decision. Inspector is now Page-property-panel only (plus future AI chat).

**Obsidian-framing reversal.** I started scrubbing Obsidian references project-wide; Nathan stopped it: > "no, don't fix any obsidian framing, just acknowledge that going forward. it's still obsidian-style." Reverted 8 edits.

**Stack recommendation.** Used context7 to verify Tiptap drag-handle is free MIT (`@tiptap/extension-drag-handle-react` ships in the regular `@tiptap/*` scope, not the paid `@tiptap-pro/*`), BlockNote XL packages confirmed copyleft-or-commercial, Apple Foundation Models is real and SwiftUI-native on macOS 26+. Recommended **SwiftUI** on four grounds: Apple-ecosystem alignment, iOS/iPad ships free same codebase, first-party Mac OS integrations (QuickLook, Share Extension, CoreSpotlight, etc.), and on-device LLM via Foundation Models.

**AI chat clarification.** Nathan: > "the AI sidebar would just be an interface for my existing CLI, I already made one with Obsiidan, it's simple and works great, it's not an APi." Foundation Models advantage downgraded — still a clean optional addition but not the planned AI chat implementation. The planned chat is a frontend to Nathan's existing local CLI (chat UI + subprocess bridge), already proven on his Obsidian setup. Updated framing across Prospects, History, PRD.

**WKWebView path documented.** Nathan: > "WKWebView blocknote is an interesting concept, i'd save that to SwiftInfo as a possible solution." Added as the third SwiftUI editor path with honest tradeoffs — native cohesion *inside the editor pane only* traded for Notion-style block UX. On-disk Markdown / SQLite / iOS-iPad parity all stay intact. Reframed SwiftInfo's "what's not on the table" section: WKWebView-hosted BlockNote/Tiptap is now an option; CodeMirror-in-WKWebView stays excluded (pays the WebView cost without buying differentiating UX). History.md SwiftUI editor strategy updated from two paths to three.

**Tiptap licensing hedging removed.** Nathan: > "fix documentation that suggests tiptap is locked behind a license— it's not." Cleaned up all "Pro-plan changes affected only..." / "no equivalent paid package" framing across six spots in Resources, History, ReactInfo, PRD.

**Final sync verified.** 14 of 16 docs identical between Studio and Nexus; 2 differ in cosmetic table-whitespace (Obsidian's auto-formatter); 1 small content diff in Domain-Model.md synced to Studio's wording.

## Where we are leaving off

Documentation is consistent across both mirrors. Stack call has a concrete recommendation (SwiftUI) with three documented editor paths on that side: fork Clearly, build original native, or host BlockNote/Tiptap in WKWebView for the Notion-style block UX. Tiptap's licensing is presented straight, the block editor UX is named as wanted on React, Items have a defined UI surface (Item window), AI chat is correctly framed as a CLI frontend, and the v1 scope has six fewer open gaps.

Next concrete activity unchanged: Nathan reviews the SwiftUI stack recommendation. Either commits, or runs an optional bounded spike (Clearly-fork assessment, or a WKWebView-hosted BlockNote PoC) before committing. After the stack decision lands, the Figma design-system build begins; v0.0 follows.
