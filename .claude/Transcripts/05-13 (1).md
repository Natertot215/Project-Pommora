### Session — 05-13 (1)

#### Focus

Continuation of the doc-restructure plan filed at `~//.claude//plans//heres-what-im-going-goofy-blossom.md` (Phases A–F locking SwiftUI as the stack + moving React content into `// ReactInfo//`). Then a sequence of conservative cleanup passes — overstatement walk-back, current-direction alignment, customization-scope tightening, simplify survey, build-readiness pass. Ended with the Xcode project scaffolded and the docs internally consistent for a v0.0 build start.

---

#### Work Done

**Phases A–F of the SwiftUI-lock restructure**

Executed the multi-phase plan from the prior session:
- **A**: Created `.claude// ReactInfo//Contingency.md` (translation methodology only) + `ReactInfo.md` folder index.
- **B**: Moved `Symbols-guide.md` (from `Guidelines`) and `v0.0.md` (from `Planning`) into `ReactInfo//`. Sliced root `Resources.md` — React section extracted to `ReactInfo//Resources.md`.
- **C**: Sliced root `.claude//ReactInfo.md` monolith into six topic files (`Editor.md`, `Spaces-DnD.md`, `Styling-Tokens.md`, `StateData.md`, `MacIntegration.md`, `Distribution.md`). Deleted source.
- **D**: Stripped all `**For React**` blocks from main docs; promoted Swift blocks to canonical; inserted cross-refs to ReactInfo. Prose-coherence cleanup pass on every modified file (D10).
- **E**: Distributed `SwiftInfo.md` content into main docs as Swift baseline (PRD got Data/State/File Watching + Mac OS Integration + Distribution subsections; Pages.md got editor depth; Spaces.md got composer depth; UIX-Guide got AppKit Interop section; History.md got research-findings appendix). Deleted root `SwiftInfo.md`. Prose-coherence cleanup (E7).
- **F**: Replaced PRD 12-row dual-stack table with Swift-only table; updated CLAUDE.md / Handoff.md / History.md / Framework.md to Swift-locked framing. Mid-pass user corrections required three rewrites of UIX-Guide / Design Guidelines / Component Guidelines / UI-UX.md: (1) Figma stuff into ReactInfo; (2) Swift doesn't use tokens (loaded context7 to verify); (3) AppKit treated as first-class alongside SwiftUI, not as escape hatch.

Phase G (simplification subagent) and Phase H (verification) from the original plan were deferred — replaced by Nathan's redirected passes below.

**Cross-check against current Apple/SwiftUI library docs (context7)**

Verified the major API surface against `/websites/developer_apple_swiftui`, `/swiftlang/swift-markdown`, `/groue/grdb.swift`. Confirmed: `NavigationSplitView(sidebar:content:detail:)` (macOS 13+), `TextEditor(text: Binding<AttributedString>, selection:)` (macOS 26+), GRDB `ValueObservation.tracking { db in ... }.values(in:)` AsyncSequence, swift-markdown's DocC `@Name(args){…}` directive syntax via `ParseOptions.parseBlockDirectives`, WebKit-for-SwiftUI on iOS 26+/macOS 26+. Identified overstatement to walk back: "confirmed top-level-only", "only credible choice", "MarkupFormatter can crash it", "Verified primitives" (none of those are how Apple's docs phrase it).

**Overstatement walk-back pass (commit `3bf6ccc`)**

Across PRD / Pages.md / Spaces.md / UIX-Guide.md / History.md — softened claims that read as if validated through actual build: NavigationSplitView "confirmed top-level-only" → "fits the three-pane shell shape"; GRDB "only credible choice" → "established choice"; "Core code pattern" → "Likely code shape"; TextKit 2 "confirmed production instability" → "community reports of rough edges"; `MarkupFormatter` "can crash it" → "reformats rather than round-trips"; "Verified primitives" → "Documented primitives"; Spaces composer library pins → candidates evaluated at build time; AppKit Interop "only achievable" → "generally requires".

**Current-direction alignment pass (commit `9a6587e`)**

History.md Stack section: dropped "Currently under re-evaluation" bullet, compressed React-path editor evaluation, retitled "Stack — Resolved: SwiftUI" → "Stack — SwiftUI" (per Nathan: "stop adding 'resolved' or history documentation where it doesnt belong"). History.md Editor section: dropped "— both stacks" / "React-conditional" framings; collapsed React-specific editor serialization architecture into a stack-agnostic principle + ReactInfo pointer. Architecture.md design-values bullet rewritten Swift-native. Dangling `// Planning//Figma Prompt.md` and `// Planning//Figma Components 5-13.md` references purged from live docs.

**Customization scope tightening (commit `1497b59`)**

Per Nathan: "In-app customization across anything besides font sizing and accent colors should be non-needed; Swift handles all of that." Scoped Framework v0.12 + PRD + History + UIX-Guide to two user-overridable values only (accent color + font size). SwiftUI handles dark mode, semantic colors, Materials, Dynamic Type natively. Added repo URL (`https://github.com/Natertot215/Project-Pommora.git`) to Handoff Branch Status.

**Simplify survey (skill-invoked, conservative)**

Dispatched three parallel agents (cross-file duplication, over-elaboration, vestigial content). Reported findings with severity grading; Nathan elected: PRD stays as-is (keep elaboration); apply tokens→values sweep + PRD hedging fixes + LOW values only.

Applied (commit `a04d78a`):
- "design tokens" → "design values" in PRD line 65, Handoff line 13, History line 29 (WKWebView-canvas occurrences kept — "tokens" is right there)
- PRD "Option 2 (likely direction)" repeated 6×: dropped four redundant instances, kept the canonical one at line 46
- PRD line 308 NavigationSplitView claim tightened
- PRD line 322 `.nexus// tabs.json` TBD parenthetical dropped
- PRD line 379 "Likely code shape" 4-line hedge → "Code shape" declarative
- Framework v0.9 board-view three-times-stated → one
- Spaces.md Custom Layout protocol h5 collapsed
- Architecture.md line 58 redundant negation merged

Settings.local.json's stale SwiftInfo.md permission entry was flagged but auto-classifier blocked self-modification of settings — left for Nathan.

**Build-readiness pass (commit `6050927`)**

Per Nathan: "Go through everything and make sure the current documentation is updated so that if I wanted to start building right now -- I could." Determined v0.0 spec doesn't need its own `// Planning//v0.0.md` — Framework v0.0 + PRD + UIX-Guide cover it. Applied:
- Framework v0.0 entry inlined: deployment target macOS 26+, window default 1200×800 + min 960×560, pane defaults (240/flex/280), what renders in v0.0
- PRD Three-Pane Shell + Top-Bar Tabs: added window dimensions; rewrote tab chrome from Figma-style `surface//` token names to "SwiftUI Material backdrop + opacity/brightness for active state"
- Handoff Active Work pivoted from "author the v0.0 spec" to "v0.0 buildable from current docs"
- Visual direction: dropped specific px ("body 14, caption 12, micro 10") — Swift uses native Font scale; specifics were Figma reference
- CLAUDE.md / Planning README / History / ReactInfo index: removed phantom `// Planning//v0.0.md` references; replaced with pointer to Framework + PRD + UIX-Guide triplet

**Residue sweep (commit `6e958eb`)** after Nathan flagged a remaining stack-uncertainty piece in Domain-Model: fixed Page-row React branch in the entity overview table; dropped PRD's "On the React path, `.nexus// symbols.json` is also seeded" sentence; reframed History.md "stack call materially" line; dropped Spaces.md "stack-portable" parenthetical.

**Push** (commit `d4ce87d`)

Nathan flagged that none of the 14 session commits had actually been pushed. Committed his pending Handoff/Contingency edits + pushed everything to `origin/main`.

**Xcode setup guide** (no commit — explanation only)

Nathan asked what to pick in each Xcode template option, especially Storage given the SQLite plan. Recommendations: macOS App template, SwiftUI interface, Swift, **Storage: None** (don't pick Core Data or SwiftData — they wrap a hidden SQLite store you can't reach into; Pommora wires its own SQLite via GRDB at v0.2). Don't enable App Sandbox at v0.0. Don't add any Swift Package dependencies until v0.2 (GRDB).

**SF Symbol weight-matching rule** (commit `33034dc`)

Added one bullet to UIX-Guide Component conventions: paired SF Symbols inherit weight from the surrounding text style; keep that link. Mixing `.thin` symbols with `.heavy` text (or the reverse) reads as inconsistent. The commit also accidentally bundled Nathan's freshly-created Xcode project scaffolding (project.pbxproj, ContentView.swift, PommoraApp.swift, Assets.xcassets, tests) under the SF Symbol rule's commit title — auto-staged from Nathan's end while the commit ran.

**Gitignore fix-forward (commit `5521c5d`)**

Untracked the two per-user `xcuserdata` files (`UserInterfaceState.xcuserstate`, `xcschememanagement.plist`) — they regenerate on every Xcode launch and would generate noise commits otherwise. Added Xcode/Swift section to `.gitignore` (`xcuserdata/`, `*.xcuserstate`, `DerivedData/`, `.swiftpm/`); React-era entries (`node_modules/`, `dist/`) kept under a "React contingency" comment.

**Accent generalization + Handoff refresh (commit `7e4c491`)**

Per Nathan: "Accent color doesnt need to be purple haha; we will go with what we have for now." Dropped "pastel-muted purple" specifics across PRD / Framework / History / UI-UX.md / Design Guidelines — accent is generic, Xcode default stands in for v0.0, brand hue picked at design lock.

Handoff refresh: Current State now includes the Xcode-project-scaffolded paragraph; Active Work pivoted to "v0.0 shell scaffolding pending" with the concrete next-step list (replace stock ContentView with NavigationSplitView, add window config, set deployment target); Open Questions trimmed to the two genuinely-open items.

---

#### Decisions

- **Stack remains SwiftUI** — no new decision, just enforced cleanup across docs.
- **No separate `// Planning//v0.0.md` spec.** Framework v0.0 entry + PRD shell sections + UIX-Guide collectively cover everything needed for a v0.0 build start.
- **Xcode project at root** of the project (sibling to `.claude//` and `UI-UX//`). macOS App template, SwiftUI, Storage: **None** (GRDB direct at v0.2, not Core Data / SwiftData).
- **Deployment target macOS 26+ (Tahoe).** Locked in PRD stack table and Framework v0.0; needed for `TextEditor(text: Binding<AttributedString>)` and SwiftUI WebView surface.
- **Window dimensions:** default 1200×800; minimum 960×560 (keeps both side panes legible).
- **In-app customization scope (Framework v0.12):** accent color + font size only. SwiftUI handles dark mode / semantic colors / Materials / Dynamic Type natively — no override needed.
- **Brand accent value deferred.** Xcode default stands in for v0.0; final accent hue picked at design lock. No longer locked to "pastel-muted purple."
- **Tab chrome rendering:** SwiftUI `Material` for the tab-row backdrop with the active tab distinguished via opacity / brightness on top — no custom color extensions for tab states.
- **SF Symbol weight matches text weight** (new convention bullet in UIX-Guide).
- **`xcuserdata/` is gitignored;** the two stale files in `33034dc` were untracked in `5521c5d`.

---

#### Session Insights (Nathan)

1. **"PRD can remain."** When the simplify survey flagged PRD acting as a maximalist superset of feature docs, Nathan chose to keep the elaboration rather than collapse it. PRD stays the canonical detailed spec; feature docs are the deep dives.

2. **"Stop adding 'resolved' or history documentation where it doesn't belong."** History.md is the right place for resolved-decision records; other docs should rewrite to current state rather than narrate the transition. Dropped "Resolved:" prefixes from section headings; rewrote content to read as if the current direction had always been the case.

3. **"Purple hex is not in-scope; that's no longer a thing now that we're with Swift."** Native SwiftUI handles the design surface via Asset Catalog + semantic colors; the specific Pommora accent hex doesn't need to be locked in planning docs — Nathan picks it in the Asset Catalog when ready. And: the accent doesn't have to be purple. Going with Xcode default for v0.0.

4. **"Swift handles all of that."** In-app customization (Framework v0.12) only needs to surface the two values a user actually picks (accent + font size). SwiftUI semantic colors / Materials / Dynamic Type / dark mode all adapt natively — no override surface needed for any of those.

5. **Contingency placement is deliberate.** Nathan added a "visual reference for the feel" line under Contingency.md's Design tokens section even though it looked out-of-place to me — flagged it intentionally for contingency-side context. Standing instruction: don't auto-revert his contingency placements.

---

#### Next Session

- **Replace Xcode's stock `ContentView.swift`** with the three-pane shell per Framework v0.0: `NavigationSplitView` sidebar / content / detail; sidebar default 240, inspector default 280; both drag-resizable; top-bar tab chrome with single non-functional placeholder tab + `+` / `×` buttons. Sidebar and inspector are empty styled surfaces (no data wiring, no nexus picker, no editor).
- **Add window config to `PommoraApp.swift`:** `.defaultSize(width: 1200, height: 800)`, `.windowResizability(.contentMinSize)` with min 960×560.
- **Set deployment target to macOS 26+** in Project Editor → Pommora target → General → Minimum Deployments (if not already).
- **`⌘R` to verify** the shell opens and panes resize correctly.
- **Open questions still open:** brand accent value (design-lock decision, post-v0.0); editor option 1 vs 2 (v0.3+ decision).
