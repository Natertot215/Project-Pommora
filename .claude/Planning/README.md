### Planning

Active plan documents live here. Completed plans are deleted once their shipped content is captured in `Framework.md` / `History.md` / `// Features//`.

**No active plans.** v0.3.0 Properties shipped 2026-05-25 (71 commits, merged to `main` as `3d1bc19`) — see `History.md` for the canonical ship summary and `Framework.md` for the roadmap.

Next plan likely lives here when v0.3.1 (Properties Pulldown + Property Panel Figma-driven polish) gets scoped.
