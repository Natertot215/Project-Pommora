/** The table-view column-header right-click menu (E-1). One action today (hide the column); the
 *  renderer applies the resolved action, or no-ops on null (dismissed). Mirrors calloutMenu's shape. */
export type ColumnMenuAction = 'column:hide'
