import { GlassEditor } from '../../glass-editor/GlassEditor'

/** Materials · Glass Lab — the folded-in CSS liquid-glass editor (its own full layout
 *  + CSS). A research surface for tuning the frost recipe against the Apple edge-lens. */
export function GlassLabLeaf(): React.JSX.Element {
  return <GlassEditor />
}
